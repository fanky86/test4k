{
  "name": "camera4k-test",
  "version": "1.0.0",
  "private": true,
  "description": "APK sederhana untuk test dukungan resolusi kamera (termasuk 4K) via WebView + getUserMedia",
  "scripts": {
    "add-android": "npx cap add android",
    "sync": "npx cap sync android",
    "open-android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/camera": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
